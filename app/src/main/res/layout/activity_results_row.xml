<?xml version="1.0" encoding="utf-8"?>
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical" android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="72dip"
            android:layout_height="72dip"
            android:scaleType="centerCrop"/>

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Card title"
            android:layout_toRightOf="@+id/thumbnail"
            android:textAppearance="@android:style/TextAppearance.Holo.Medium"
            android:layout_marginLeft="?android:attr/listPreferredItemPaddingLeft"/>


    <!-- PREGUNTA 3.1.
    AÑADIR EL PRECIO
    Agregamos al layout un elemento de tipo "TextView" con el ID "price".
    Este "TextView" usa la propiedad "layout_toRigthOf" para ubicarse al lado
    derecho del elemento que contiene la imagen del producto llamado
    "thumbnail",     y utiliza la propiedad "layout_below" para ubicarse debajo
    del TextView "title" que contiene el título del producto. -->
        <TextView
            android:id="@+id/price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Card price"
            android:layout_toRightOf="@+id/thumbnail"
            android:layout_below="@+id/title"
            android:textAppearance="@android:style/TextAppearance.Holo.Small"
            android:layout_marginLeft="?android:attr/listPreferredItemPaddingLeft"/>


    </RelativeLayout>